# 🎯 استخدام Mock Data - الاختبار بدون Supabase

> **تم حل المشكلة!** الآن يمكنك اختبار التطبيق بدون الحاجة لحساب Supabase

## 📋 ما هي Mock Data؟

**Mock Data** = بيانات وهمية مدمجة في التطبيق للاختبار والتطوير

✅ لا تحتاج لحساب Supabase
✅ سريع جداً - لا إنترنت مطلوب
✅ يمكنك اختبار جميع الميزات
✅ سهل التبديل للـ Supabase الحقيقية لاحقاً

---

## 🚀 للبدء فوراً

### 1️⃣ التطبيق مُعد بالفعل!

```bash
flutter run
```

التطبيق سيستخدم تلقائياً بيانات وهمية ✨

---

## 📱 حسابات الاختبار الجاهزة

استخدم أي من هذه الحسابات للدخول:

### 👤 حساب مريض

```
البريد: patient@example.com
كلمة المرور: Password123
الدور: مريض ✅
```

### 👨‍⚕️ حساب طبيب

```
البريد: doctor@example.com
كلمة المرور: Password123
الدور: طبيب ✅
```

---

## 🔄 لماذا تعمل الآن؟

### المحتويات المُعدة:

| الملف                              | الدور               |
| ---------------------------------- | ------------------- |
| `mock_auth_datasource.dart`        | مصادقة وهمية        |
| `mock_doctor_datasource.dart`      | بيانات أطباء وهمية  |
| `mock_appointment_datasource.dart` | بيانات مواعيد وهمية |
| `mock_auth_service.dart`           | خدمة مصادقة للـ UI  |

### البيانات الوهمية المتضمنة:

#### 👥 الأطباء:

1. **د. محمود أحمد** - طب عام (4.8⭐)
2. **د. فاطمة السلمان** - طب أطفال (4.9⭐)
3. **د. أحمد العنزي** - جراحة عامة (4.6⭐)
4. **د. نور الجابري** - أمراض النساء (4.7⭐)

#### 📅 المواعيد:

- 2 موعد مع الأطباء
- حالات مختلفة: `Scheduled`, `Completed`

---

## 🔧 التبديل بين Mode الاختبار والإنتاج

### في `lib/main.dart`:

```dart
// ============================================
// وضع التطوير: استخدم Mock Data
// ============================================
// اجعلها true لاختبار التطبيق بدون Supabase
const bool USE_MOCK_DATA = true;  // ← غيّر هذا
// ============================================
```

#### لاستخدام Supabase الحقيقية:

```dart
const bool USE_MOCK_DATA = false;  // ✅ Supabase الحقيقية
```

#### لاستخدام Mock Data:

```dart
const bool USE_MOCK_DATA = true;  // ✅ بيانات وهمية
```

> **ملاحظة**: عند التبديل، احفظ الملف وشغّل `flutter run`

---

## 🧪 ماذا تستطيع أن تختبر الآن؟

✅ **التسجيل الجديد** - اختر دور (مريض/طبيب)
✅ **تسجيل الدخول** - استخدم البيانات المذكورة أعلاه
✅ **لوحة الطبيب** - عرض إعدادات العيادة والمواعيد
✅ **حجز المواعيد** - اختيار طبيب وتاريخ ووقت
✅ **التنقل** - الانتقال بين الشاشات

❌ **لا تُحفظ البيانات** - تُفقد عند إعادة التشغيل (هذا طبيعي!)

---

## 🔍 كيف تتحقق من الـ Debug؟

### في Console:

عند تسجيل دخول ستشاهد مثلاً:

```
🟡 وضع التطوير: استخدام بيانات وهمية (Mock Data)
🔐 Mock Sign In: patient@example.com
✅ Mock Sign In Success: patient-001 (patient)
```

---

## 📊 البيانات المُعدة بالتفصيل

### جدول المستخدمين (Profiles):

```
┌─────────────────────────────────────────────────┐
│ البريد                │ الدور   │ المعرّف     │
├─────────────────────────────────────────────────┤
│ patient@example.com   │ مريض  │ patient-001 │
│ doctor@example.com    │ طبيب  │ doctor-001  │
└─────────────────────────────────────────────────┘
```

### جدول الأطباء (Doctors):

```
┌────────────────────────────────────────────────────────┐
│ الاسم              │ التخصص    │ التقييم │ ساعات العمل │
├────────────────────────────────────────────────────────┤
│ د. محمود أحمد      │ طب عام   │ 4.8  │ 09:00-17:00 │
│ د. فاطمة السلمان   │ طب أطفال │ 4.9  │ 10:00-16:00 │
│ د. أحمد العنزي     │ جراحة   │ 4.6  │ 08:00-14:00 │
│ د. نور الجابري     │ النساء   │ 4.7  │ 09:00-17:00 │
└────────────────────────────────────────────────────────┘
```

### جدول المواعيد (Appointments):

سيتم إنشاء مواعيد احتبارية عند الحجز

---

## ❓ الأسئلة الشائعة

### س: هل تُحفظ البيانات التي أدخلها؟

**ج**: لا، البيانات الوهمية مؤقتة فقط لهذه الجلسة. عند إعادة التشغيل تُرجع إلى الحالة الأولية.

### س: كيف أنتقل للـ Supabase الحقيقية؟

**ج**:

1. اجعل `USE_MOCK_DATA = false`
2. ادخل بيانات Supabase في `main.dart`
3. شغّل `flutter run`

### س: هل يمكنني تعديل البيانات الوهمية؟

**ج**: نعم! عدّل:

- `lib/data/datasources/mock_auth_datasource.dart` - للمستخدمين
- `lib/data/datasources/mock_doctor_datasource.dart` - للأطباء
- `lib/data/datasources/mock_appointment_datasource.dart` - للمواعيد

### س: ماذا إذا أردت Supabase بعد التطوير؟

**ج**: لا تقلق! الكود مصمم للتبديل السهل:

- الـ Mock datasources موازية للحقيقية
- لا تحتاج لتغيير الـ UI أو Business Logic

---

## 🎬 مثال الاستخدام

### تسجيل دخول كمريض:

```
1. اضغط "تسجيل جديد"
2. ادخل:
   - البريد: patient@example.com
   - كلمة المرور: Password123
   - الاسم: أحمد علي
   - اختر: مريض
3. اضغط "تسجيل"
4. ✅ ستدخل لشاشة حجز المواعيد
```

### تسجيل دخول كطبيب:

```
1. سجّل دخول خروج أولاً
2. اضغط "تسجيل جديد"
3. ادخل:
   - البريد: doctor@example.com
   - كلمة المرور: Password123
   - الاسم: د. محمود
   - اختر: طبيب
4. اضغط "تسجيل"
5. ✅ ستدخل لوحة تحكم الطبيب
```

---

## ⚙️ التفاصيل التقنية

### البنية:

```
lib/
├── data/datasources/
│   ├── mock_auth_datasource.dart ✅
│   ├── mock_doctor_datasource.dart ✅
│   ├── mock_appointment_datasource.dart ✅
│   ├── auth_datasource.dart (Supabase الحقيقية)
│   ├── doctor_datasource.dart
│   └── appointment_datasource.dart
├── presentation/providers/
│   ├── mock_auth_service.dart ✅
│   └── auth_service.dart (Supabase)
└── main.dart (selector) ✅
```

### كيف يعمل الاختيار:

```dart
// في main.dart
ChangeNotifierProvider(
  create: (_) => USE_MOCK_DATA
      ? MockAuthService()        // ✅ للتطوير
      : AuthService(...),        // ✅ للإنتاج
)
```

---

## 🎉 الخطوات التالية

1. **شغّل التطبيق الآن**: `flutter run`
2. **اختبر Mockبيانات الجاهزة**
3. **اكتشف جميع الميزات**
4. **عندما تريد Supabase**: قل لي وسأساعدك

---

**وقت البدء**: الآن! ✨
