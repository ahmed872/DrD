{{flutter_js}}
{{flutter_build_config}}

// مُقلِع Flutter مخصّص لتطبيق DrD.
//
// ## لماذا نتجاوز الملف الافتراضي؟
//
// المُقلِع الذي يولّده Flutter يمرّر `serviceWorkerSettings`، فيسجّل
// `flutter_service_worker.js` على النطاق الجذر `/`. وذلك الملف — في إصدارات
// Flutter الحديثة — لم يعد يخزّن شيئاً، بل يستدعي `registration.unregister()`
// فور تفعيله (وهو نفسه يصف نفسه بأنه مهمل وسيُحذف لاحقاً).
//
// المشكلة أن `web/sw.js` الخاص بنا يُسجَّل على النطاق الجذر أيضاً. تسجيل
// نصّين مختلفين على نفس النطاق يستبدل أحدهما الآخر — فينتهي الأمر بأن يحلّ
// عامل Flutter محلّ عاملنا ثم يُلغي نفسه، فلا يبقى أي عامل خدمة، ويفقد
// التطبيق دعم العمل بلا اتصال وشروط تثبيت PWA بصمت تام.
//
// حذف `serviceWorkerSettings` هنا يمنع Flutter من التسجيل أصلاً، ويترك
// السيطرة الكاملة لـ `sw.js` المسجَّل من `index.html`.

_flutter.loader.load();
